# Pinned exactly, and every version is at least 14 days old at the time of pinning.
fastapi==0.141.1
uvicorn==0.52.4
# Scraping the portal: HTTP session with browser impersonation and cookie persistence.
# The [fetchers] extra is required even for the plain HTTP session - curl_cffi lives
# there. Browser-based fetchers would additionally need a one-off `scrapling install`
# to download a browser; consumption/source.py deliberately avoids needing that.
scrapling[fetchers]==0.4.15
# Windows ships no timezone database; zoneinfo needs this for Asia/Jerusalem.
tzdata==2026.3
# FastAPI TestClient (Starlette) needs an HTTP client for the self-checks.
httpx==0.28.1
# Home Assistant recorder/import_statistics over the Supervisor WebSocket proxy.
websockets==17.1
